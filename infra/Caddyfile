# Apex/control plane is hosted separately. Wildcard DNS points at this gateway.
# Caddy's ask endpoint only permits existing published hosts and verified domains.
{
  email {$ACME_EMAIL}
  on_demand_tls {
    ask http://127.0.0.1:8090/_shelby/tls-ask
  }
}
https:// {
  tls {
    on_demand
  }
  reverse_proxy 127.0.0.1:8090
}
